#pragma once
#include "Person.h"
#include "Abiturient.h"
#include "Vector.h"
#include <iostream>
using namespace std;

int main()
{
    // Person *a = new Person; //создаем объект класса Person
    // cout << " Enter data Person";
    // a -> Input();
    // Abiturient *b = new Abiturient;
    // cout << " Enter data Abiturient"; //создаем объект класса Abiturient
    // b -> Input();
    // Vector v(10); //Создаем вектор
    // Object *p = a; //ставим указатель на объект класса Person
    // v.Add(); //добавляем объект в вектор
    // p = b; //ставим указатель на объект класса Abiturient
    // v.Add(); //добавляем объект в вектор
    // v.Show(); //вывод вектора
    // v.Del(); //удаление элемента
    // cout << "\n Vector size=" <<  v(); 
    Dialog D;
    D.Execute();
return 0;
}

