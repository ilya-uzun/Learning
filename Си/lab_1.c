main(){
    int n =0, m=0;
   int s = sum(n,m);
 printf(s);
}

int sum (int n,int m){
   int summa 0;
   for (int i=0,n = i, i++)summa++;
   if (n = summa){
   for (int j=0,m = j, j++)summa++;
   return summa;
}
